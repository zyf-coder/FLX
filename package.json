{
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:app": "vite build --base ./ && node -e \"require('fs').rmSync('dist/downloads',{recursive:true,force:true})\"",
    "android:sync": "npm run build:app && cap sync android",
    "android:apk": "npm run android:sync && cd android && gradlew.bat assembleDebug",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/app": "^5.0.8",
    "@capacitor/browser": "^5.2.1",
    "@capacitor/cli": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "@capacitor/ios": "^5.7.8",
    "@capacitor/local-notifications": "^5.0.7",
    "lucide": "0.468.0",
    "vite": "4.5.14",
    "vue": "2.7.16"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
